FROM node:20-alpine
RUN apk add --no-cache bash && npm install -g luamin
WORKDIR /build
COPY serialize.sh mod.lua mod_buildings.lua ./
RUN sed -i 's/\r$//' serialize.sh && chmod +x serialize.sh
ENTRYPOINT ["bash", "./serialize.sh"]
